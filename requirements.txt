# ── Core deep-learning stack ──────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0

# ── Segmentation models (UNet + ResNet50 encoder) ─────────────────────────────
segmentation-models-pytorch>=0.3.3
timm>=0.9.0                   # encoder backbone hub used by smp

# ── Data I/O ──────────────────────────────────────────────────────────────────
zarr>=2.16.0,<3.0.0           # v3 has breaking API changes; dataset is zarr-v2
pyarrow>=14.0.0               # parquet reader
pandas>=2.0.0
numpy>=1.24.0

# ── Training utilities ────────────────────────────────────────────────────────
tensorboard>=2.14.0
tqdm>=4.66.0
pyyaml>=6.0

# ── Notebook / EDA ────────────────────────────────────────────────────────────
matplotlib>=3.7.0
notebook>=7.0.0
ipykernel>=6.0.0

# ── Optional: future ViT / Prithvi models ────────────────────────────────────
# Pin to <4.40 so it stays compatible with torch 2.1.x in the base image.
# Bump together with the PyTorch base image when upgrading.
transformers>=4.35.0,<4.40.0  # HuggingFace; used by Prithvi adapter (Stage 2)

# ── Clay foundation model (vendored encoder; einops is the only new dep) ─────
einops>=0.7.0                 # tensor ops used by vendored Clay encoder code

# ── Optional: future XGBoost baseline ────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.3.0
